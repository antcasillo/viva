# Backend SQLite (default in dev: http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# Produzione: URL del tuo server
# VITE_API_URL=https://tuodominio.it

# JWT (solo server, in .env)
# JWT_SECRET=cambia-in-produzione
